{
  "name": "smart-inventory-control",
  "version": "0.1.0",
  "private": true,
  "description": "Smart Inventory Control. Reports on the existing ledsone inventory database, read-only.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node inventory/server.js",
    "test": "node --test --test-concurrency=1 \"inventory/**/*.test.js\""
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
